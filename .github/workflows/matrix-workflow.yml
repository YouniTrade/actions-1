name: Matrix workflow
on:
  workflow_dispatch:
  push:

jobs:
  ubuntu-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Display dummy message
        run: echo "Installing on ubuntu"
  windows-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Display dummy message
        run: echo "Installing on windows"
